<?xml version="1.0" encoding="utf-8"?>
<!-- Author: Claude (Anthropic) - Security hardening: allowBackup=false, BootReceiver unexported, ShareReceiverActivity permission -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Foreground service for auto mode (screenshots) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".GigLensApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="GigLens"
        android:supportsRtl="true"
        android:theme="@style/Theme.GigLens">

        <!-- Firebase: Crashlytics enabled, ad tracking disabled -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="true" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />

        <!-- Main activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- DEFAULT: Share target — receives screenshots from any app -->
        <activity
            android:name=".ui.SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.AccessibilityDisclosureActivity"
            android:label="Accessibility Permission"
            android:exported="false"
            android:parentActivityName=".ui.SettingsActivity" />

        <activity
            android:name=".ui.ShareReceiverActivity"
            android:exported="true" android:theme="@style/Theme.GigLens.Transparent"
            android:label="GigLens">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- AUTO MODE (opt-in): Accessibility Service -->
        <service
            android:name=".service.OfferDetectorService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- AUTO MODE: Screenshot capture service -->
        <service
            android:name=".service.ScreenCaptureService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />

        <!-- Boot receiver — auto-starts widget if enabled -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Accessibility offer receiver — scores offers extracted from accessibility tree -->
        <receiver
            android:name=".service.AccessibilityOfferReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.augusteenterprise.giglens.OFFER_EXTRACTED" />
            </intent-filter>
        </receiver>

        <!-- Town accuracy confirmation — handles Yes/No notification taps -->
        <receiver
            android:name=".service.TownAccuracyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.augusteenterprise.giglens.TOWN_CONFIRMED" />
                <action android:name="com.augusteenterprise.giglens.TOWN_WRONG" />
            </intent-filter>
        </receiver>
        <!-- DEBUG ONLY: ADB trigger receiver for testing widget states -->
        <receiver
            android:name=".service.DebugTriggerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.augusteenterprise.giglens.DEBUG_TRIGGER" />
            </intent-filter>
        </receiver>

        <!-- OVERLAY: Floating pill widget over DoorDash -->
        <service
            android:name=".service.OfferOverlayService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />
    </application>
</manifest>
