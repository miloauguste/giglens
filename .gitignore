local.properties

# Build outputs
app/build/
build/
.gradle/

# Security scan reports (local only)
security_reports/

# Keystore files
*.jks
*.keystore
keystore.properties
giglens-release.keystore*
key.sh
out
claude_tranisiot.md
"Enable Screen Capture"
"Failed"
"Get"
"Run"
"Screen Capture Active"
Enable Screen Capture
Failed
Get
Run
Screen Capture Active

# Archived session transcripts
docs/transcripts/

# Security scan reports
docs/security-reports/
.pixel_port

# Session scratch artifacts — never commit (added 2026-06-17)
*.backup.*
*.broken_today
/patch_*.py
/create_giglens_debug_email.py
/smptdefault.py

# Runtime debug output — not source
docs/*.log
docs/*.db

# Google Play service account credential — NEVER commit
google-play-api-key.json
docs/shift_logs/
